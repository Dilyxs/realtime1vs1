const PlayerReadyOption = ({ ws }) => {
  return (
    <div>
      <h1>Are you ready?</h1>
      <button onClick={() => {}}>No</button>
      <button>Yes</button>
    </div>
  );
};

export default PlayerReadyOption;
